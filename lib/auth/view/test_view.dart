import 'package:flutter/material.dart';

class TestView extends StatelessWidget {
  const TestView({super.key});
  static String route = 'test-view';
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
